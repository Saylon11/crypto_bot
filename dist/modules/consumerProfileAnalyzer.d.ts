import { WalletData, ConsumerProfile } from "../types";
/**
 * Analyze active wallets to classify consumer profiles (Shrimp, Dolphin, Whale).
 */
export declare function analyzeConsumerProfiles(wallets: WalletData[]): ConsumerProfile;
